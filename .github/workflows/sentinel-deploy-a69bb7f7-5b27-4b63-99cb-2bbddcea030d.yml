name: Deploy Content to sentinel-test-01 [a69bb7f7-5b27-4b63-99cb-2bbddcea030d]
# Note: This workflow will deploy everything in the root directory.
# To deploy content only from a specific path (for example SentinelContent):
#   1. Add the target path to the "paths" property like such
#    paths:
#    - 'SentinelContent/**'
#    - '!.github/workflows/**'
#    - '.github/workflows/sentinel-deploy-a69bb7f7-5b27-4b63-99cb-2bbddcea030d.yml'
#   2. Append the path to the directory environment variable below
#       directory: '${{ github.workspace }}/SentinelContent'

on:
  push:
    branches: [ main ]
    paths:
    - '**'
    - '!.github/workflows/**'  # this filter prevents other workflow changes from triggering this workflow
    - '.github/workflows/sentinel-deploy-a69bb7f7-5b27-4b63-99cb-2bbddcea030d.yml'

jobs:
  deploy-content:
    runs-on: windows-latest
    env:
      resourceGroupName: 'rg-sentinel-test'
      workspaceName: 'sentinel-test-01'
      workspaceId: '41f155b3-16e0-4114-b56a-64ec1b44c582'
      directory: '${{ github.workspace }}'
      cloudEnv: 'AzureCloud'
      contentTypes: 'AnalyticsRule'
      branch: 'main'
      sourceControlId: 'a69bb7f7-5b27-4b63-99cb-2bbddcea030d'
      rootDirectory: '${{ github.workspace }}'
      githubAuthToken: ${{ secrets.GITHUB_TOKEN }}
      smartDeployment: 'true'
    permissions:
      contents: write
      id-token: write # Require write permission to Fetch an OIDC token.

    steps:
    - name: Login to Azure (Attempt 1)
      continue-on-error: true
      id: login1
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_a69bb7f75b274b6399cb2bbddcea030d }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_a69bb7f75b274b6399cb2bbddcea030d }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_a69bb7f75b274b6399cb2bbddcea030d }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 1 failed
      if: ${{ steps.login1.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 2)
      continue-on-error: true
      id: login2
      uses: azure/login@v2
      if: ${{ steps.login1.outcome=='failure' }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_a69bb7f75b274b6399cb2bbddcea030d }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_a69bb7f75b274b6399cb2bbddcea030d }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_a69bb7f75b274b6399cb2bbddcea030d }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Wait 30 seconds if login attempt 2 failed
      if: ${{ steps.login2.outcome=='failure' }}
      run: powershell Start-Sleep -s 30

    - name: Login to Azure (Attempt 3)
      continue-on-error: false
      id: login3
      uses: azure/login@v2
      if: ${{ steps.login2.outcome=='failure'  }}
      with:
        client-id: ${{ secrets.AZURE_SENTINEL_CLIENTID_a69bb7f75b274b6399cb2bbddcea030d }}
        tenant-id: ${{ secrets.AZURE_SENTINEL_TENANTID_a69bb7f75b274b6399cb2bbddcea030d }}
        subscription-id: ${{ secrets.AZURE_SENTINEL_SUBSCRIPTIONID_a69bb7f75b274b6399cb2bbddcea030d }}
        environment: 'AzureCloud'
        audience: api://AzureADTokenExchange
        enable-AzPSSession: true

    - name: Checkout
      uses: actions/checkout@v3

    - name: Resolve KQL query files into ARM templates
      shell: pwsh
      run: |
        & "${{ github.workspace }}/Resolve-KqlQueries.ps1" -InputPath "${{ github.workspace }}/AnalyticRules"
        # Commit resolved files locally so the deploy script's git operations
        # (branch switching for smart deployment tracking) don't fail on dirty working tree.
        # This commit stays on the CI runner only — it is never pushed.
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"
        git add -A
        git commit -m "ci: resolve kql query placeholders [skip ci]" --allow-empty

    - name: Deploy Content to Microsoft Sentinel
      uses: azure/powershell@v2
      with:
        azPSVersion: 'latest'
        inlineScript: |
          ${{ github.workspace }}//.github/workflows/azure-sentinel-deploy-a69bb7f7-5b27-4b63-99cb-2bbddcea030d.ps1